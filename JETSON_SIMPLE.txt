# JETSON - PULL & RUN - JEDNODUCHO

## Na Jetsone - Spusti Toto:

### PRVÝ RÁZ (Len raz)
```bash
cd ~/Desktop
git clone https://github.com/nehovortake/VisionPilot-XR-Linux.git
```

### VŽDY PRED SPUSTENÍM

```bash
cd ~/Desktop/VisionPilot-XR-Linux
git pull origin main
export LD_PRELOAD=/usr/lib/aarch64-linux-gnu/libgomp.so.1
python3 main.py
```

---

## ALEBO CEL SKRIPT V JEDNOM

```bash
cd ~/Desktop && \
([ -d VisionPilot-XR-Linux ] || git clone https://github.com/nehovortake/VisionPilot-XR-Linux.git) && \
cd VisionPilot-XR-Linux && \
git pull origin main && \
export LD_PRELOAD=/usr/lib/aarch64-linux-gnu/libgomp.so.1 && \
python3 main.py
```

---

## CO SA STANE

1. ✅ Stiahneme kód z GitHub
2. ✅ Nainštalujeme dependencies
3. ✅ Nastavíme PyTorch fix
4. ✅ Spustíme aplikáciu

Vidíš output:
```
Vehicle speed: 45 km/h | Detected sign: Yes | Read sign: 50 km/h
```

---

**To je všetko!** 🎯

